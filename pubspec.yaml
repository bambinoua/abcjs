name: abcjs
description: "A set of classes to implement ABC Notation (ABC.js) in flutter"
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
